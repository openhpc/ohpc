# AlmaLinux configuration

distro: "almalinux"
is_almalinux: true

distro_name: "AlmaLinux"
distro_image: "almalinux"

# Computed values (derived from distro_name + distro_version)
# Note: distro_version comes from el10.yaml, duplicated here for clarity
distro_full_name: "AlmaLinux 10"
baseos: "almalinux-10"
baseosshort: "almalinux10"
distro_container_image: "almalinux:10"

# URLs
distro_download_page: "https://almalinux.org/get-almalinux/"
distro_base_url: "https://repo.almalinux.org/almalinux/"
