Installation is accomplished in two steps: First, a generic OS image is
installed on *compute* nodes and then, once the nodes are up and running,
OpenHPC components are added to both the head node and the nodes at the same
time.
