TESTS_ENVIRONMENT = BATS_NO_SUMMARY=1

TESTS             = build
TESTS            += rm_execution_serial
TESTS            += rm_execution_openmp
TESTS            += rm_execution_mpi
TESTS            += rm_execution_mpiopenmp
TESTS            += rm_execution_mpirma
TESTS            += rm_execution_mpishm
