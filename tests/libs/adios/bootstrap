#!./common/bats/bin/bats
# -*-sh-*-

load   ./common/test_helper_functions || exit 1
source ./common/functions             || exit 1
source ./common/TEST_ENV              || exit 1
source ./LOCAL_ENV                    || true

@test "[$TEST_DIR] bootstrap" {
	./common/../bootstrap
}
