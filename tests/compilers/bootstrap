#! /bin/sh
# --------------------------------------------------------------------------
# bootstrapping utility for autotools -> now just uses autoreconf
# -------------------------------------------------------------------------- 

cmd="autoreconf -f -i"
echo "Bootstrapping using $cmd ..."

$cmd